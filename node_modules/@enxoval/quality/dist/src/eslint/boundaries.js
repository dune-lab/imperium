"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.makeBoundaries = makeBoundaries;
const eslint_plugin_boundaries_1 = __importDefault(require("eslint-plugin-boundaries"));
function makeBoundaries(layers) {
    return [
        {
            files: ['src/**/*.ts'],
            plugins: { boundaries: eslint_plugin_boundaries_1.default },
            settings: {
                'boundaries/elements': layers.map(({ name, pattern }) => ({ type: name, pattern })),
            },
            rules: {
                'boundaries/element-types': [
                    'error',
                    {
                        default: 'disallow',
                        rules: layers.map(({ name, allow }) => ({ from: name, allow })),
                    },
                ],
            },
        },
    ];
}
//# sourceMappingURL=boundaries.js.map