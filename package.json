{
  "name": "imperium",
  "version": "1.0.0",
  "license": "MIT",
  "prettier": "@enxoval/quality/prettier",
  "scripts": {
    "dev": "nodemon",
    "build": "tsc",
    "start": "node --env-file=.env dist/src/server.js",
    "lint": "prettier --check src && eslint src --max-warnings 0",
    "lint-fix": "prettier --write src && eslint src --fix",
    "postbuild": "node node_modules/@enxoval/http/dist/cli/generate-contracts.js"
  },
  "dependencies": {
    "@enxoval/auth": "^1.0.0",
    "@enxoval/http": "^1.0.10",
    "@enxoval/observability": "^1.0.1",
    "@enxoval/types": "^1.0.6"
  },
  "devDependencies": {
    "@enxoval/quality": "^1.0.1",
    "@types/node": "^24.0.0",
    "nodemon": "^3.1.14",
    "pino-pretty": "^13.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.2"
  }
}
